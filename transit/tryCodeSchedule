sample for retrieving schedule:

CONSIDER CHANGING SCHEDULE TO DATA

schedule = JSON.parse(the string)
//to print line
//console.log(schedule.line);

//Step 1: go through each train
for (i = 0; i < schedule["schedule"].length){
	destination = data["schedule"][i];
	//step 2: get list of stops
	stops = destination["Predictions"];
	for (j = 0; j < stops.length; j++){
		s = stops[j];
		if (s == stop_of_interest) {
			console.log(s["Seconds"]);
			console.log(destination["Destination"]);
		}
	}
}



/*info window should have
Davis
line||trip#||Destination||TimeRemaining   
Red    12     Alewife      Some time
Red    12     Ashmont      Some time 
etc.................................
*/